{
  "id": "narrations-invalid-marker",
  "level": "L2",
  "description": "Invalid narrations marker case: unclosed brackets and unknown markers exist but case expects them clean, so eval should fail.",
  "fixture": "fixtures/chapter-invalid",
  "expected": {
    "phase": "phase3-audio",
    "contracts": {
      "marker_format": {
        "known_markers_only": true,
        "properly_closed": true,
        "no_nesting": true,
        "allowed_markers": ["停顿", "重音", "语速"]
      }
    }
  },
  "timeout_ms": 30000
}
